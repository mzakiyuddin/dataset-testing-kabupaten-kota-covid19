name: Download data

on: push

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo content
        uses: actions/checkout@v2

      - name: Run script
        id: runscript
        run: |
          ./script/download_data
          ls *
